[package]
name = "silero-vad-crs"
version = "0.1.0"
edition = "2024"
description = "Self-contained static Rust FFI wrapper for Silero VAD C"
license = "MIT"
build = "build.rs"

[dependencies]

[build-dependencies]
cc = "1.2"

[features]
default = []
sse = []
avx2 = []
neon = []
fast-math = []
